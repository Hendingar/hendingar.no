node_modules/
.env
.env.local
.DS_Store
.svelte-kit/
build/
dist/
test-results/
playwright-report/

# vitest browser mode drops these beside a failing component spec: a screenshot of the failure and
# whatever the run attached to it. Debugging aids for the run that produced them, not artefacts.
__screenshots__/
.vitest-attachments/
*.tsbuildinfo

# symlink to the root .env, created by scripts/db.sh
app/.env

# git worktrees for parallel agents; `git worktree` places them inside the repo
.claude/worktrees/

# python venv for services/verifier
.venv/
__pycache__/
*.egg-info/
.pytest_cache/
.ruff_cache/
