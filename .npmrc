# Fail loudly instead of silently resolving a different version.
engine-strict=true
# Workspace packages must resolve to the local copy, never the registry.
link-workspace-packages=true
save-workspace-protocol=rolling
